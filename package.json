{
  "name": "faction-farm-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Bot Discord para registro de farm por facção",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "start:web": "node src/web.js",
    "build:web": "npm run build --prefix web",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "discord.js": "^14.22.1",
    "dotenv": "^16.6.1"
  }
}
